import React, { FC, ReactElement } from "react";

interface Props {

}

const Window: FC<Props> = (): ReactElement => {
  return (
    <React.Fragment>
      <h1>default bucket</h1>
    </React.Fragment>
  );
};

export default Window;
