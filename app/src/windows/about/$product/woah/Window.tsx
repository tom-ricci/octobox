import React, { FC, ReactElement } from "react";
import { MetaTags } from "../../../../components/routing/MetaTags";

interface Props {

}

const Window: FC<Props> = (): ReactElement => {
  return (
    <React.Fragment>

    </React.Fragment>
  );
};

export default Window;

export const Tags: MetaTags = {
  title: "aaa"
};
