import React, { FC, ReactElement } from "react";

interface Props {

}

/**
 * Manages the head of the document via a React portal.
 * @constructor
 */
export const HeadManager: FC<Props> = (): ReactElement => {
  return (
    <React.Fragment>

    </React.Fragment>
  );
};
